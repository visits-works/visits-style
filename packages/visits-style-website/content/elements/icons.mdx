---
title: Icons
---

import * as Icons from '@components/Icons';

<section style={{ display: 'flex', marginTop: '2rem' }}>
  {Object.keys(Icons).map(key => {
    const Icon = Icons[key];
    return (
      <div key={key} style={{ textAlign: 'center', flex: 1, margin: '1rem' }}>
        <Icon />
        <span style={{ display: 'block', marginTop: '0.85rem' }}>{key}</span>
      </div>
    );
  })}
</section>
